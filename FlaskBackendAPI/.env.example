# PUBLIC_INTERFACE
# Flask Backend API environment variables
PORT=5000
APP_VERSION=0.1.0

# MongoDB connection info (used by future database integration)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=network_inventory
MONGODB_COLLECTION=devices
