# FlaskBackendAPI environment variables
# Copy this file to `.env` and adjust as needed.

# Server configuration
PORT=5000
APP_VERSION=0.1.0

# CORS configuration
# In development, the React app typically runs on http://localhost:5173 (Vite default)
# Adjust if your frontend serves from a different origin.
CORS_ALLOW_ORIGINS=http://localhost:5173

# Secret key for sessions/signing (use a secure value in non-dev)
SECRET_KEY=dev-insecure-change-me

# MongoDB connection settings (for future CRUD integration)
# MONGODB_URI=mongodb://root:example@localhost:27017/inventory?authSource=admin
# MONGODB_DB_NAME=inventory
# MONGODB_COLLECTION_DEVICES=devices
# MONGODB_COLLECTION_LOGS=logs
